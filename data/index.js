// Scripted by DeltaX8

var userInput = document.getElementById("assignmentInput").value;
var code = "omega10x";

function AddToList()
{
 if (userInput == code)
 {
  window.location.replace("data/main.html"); 
 }
}
